{
  "name": "@fin-tools/python-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "source ./.venv/bin/activate && uvicorn main:app --reload",
    "win:dev": ".venv\\Scripts\\activate.bat && uvicorn main:app --reload",
    "prepare-app":"python -m venv .venv && source ./.venv/bin/activate && pip install -r requirements.txt",
    "win:prepare-app":"python -m venv .venv && .venv\\Scripts\\activate.bat && pip install -r requirements.txt"
  }
}
